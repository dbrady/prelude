;; new-emacs-troubleshooting.el
;;
;; If you try to install something with package-install and get weird FTP errors
;; looking for an outdated version of the package, you need to run:
;;
;; M-x package-refresh-contents
;;
;; (package-refresh-contents) <- C-x C-e
